let t = 800;
let md = 4;
let tiwl = 0;

function myFunction() {
	//too much?
	if (t > 730) {
	//yes
		//did i miss it?
		//am i lucky?
		//no
		if (Math.random() > 0.5) {
			//do i care?
			if((md >= 2 || tiwl >= 4) || t > 800) {
				return 'no';
				//or do i?
			} else if(md < 2 || td < 4) {
				return 'yes';
				// or do i?
			} else {
				return 'yes';
			}
			//am i late?
		} 
		// no
		else {
			return 'yes'
		}
	}
	// no i dont
	else {
		//did i miss the bus?
		return 'yes';
	}
}

console.log(myFunction());